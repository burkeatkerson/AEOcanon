---
term: "Large Language Model (LLM)"
slug: large-language-model
definition: A large language model is an AI system trained on vast amounts of text to predict and generate language, and is the engine that writes the answers in AI search.
aka:
  - LLM
related:
  - answer-engine
  - training-data
  - transformer
  - hallucination
pillars:
  - extractability
author: priya-nair
published: 2026-06-09
updated: 2026-06-09
schemaType: Article
---

**A large language model is the AI that generates the answer.** Trained on enormous
volumes of text to predict the next word in context, an LLM can write fluent,
coherent responses — and in an [answer engine](/glossary/answer-engine) it's the
component that turns retrieved passages into the final, synthesized reply.

For AEO, the key insight is that an LLM doesn't reproduce your page; it reads the
passages it's given and rewrites an answer, often quoting or paraphrasing the
clearest source. That's why [extractable](/pillars/extractability), self-contained
writing wins — it's the easiest for the model to lift accurately and attribute. It's
also why models can [hallucinate](/glossary/hallucination): left without good
retrieved sources, they generate plausible-sounding text from memory.

**Example.** When an answer engine responds to your query, an LLM is doing the
writing — taking the retrieved sources and composing a reply. Your job in AEO is to
make your passage the one it finds easiest to use and credit.
